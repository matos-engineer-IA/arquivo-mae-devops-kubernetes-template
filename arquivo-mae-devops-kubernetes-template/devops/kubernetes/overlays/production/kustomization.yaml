apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app-template-production

resources:
  - ../../base
  - poddisruptionbudget.yaml

configMapGenerator:
  - name: app-config
    behavior: merge
    literals:
      - APP_ENV=production
      - LOG_LEVEL=info

patches:
  - target:
      kind: Deployment
      name: app-template
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
